# 商談ロープレAIのTTSとSTTとLLMの処理の部分
このバックエンドフォルダではロープレAIが
- ユーザーからの音声をSTT(Speech To Text)する処理
- そのテキストとペルソナを基にLLMが回答を生成する処理
- その回答をTTS(Text To Speech)する処理
を行い、そのデータをフロントエンド側に返すという処理を行います

全体の要件定義に関しては要件定義.mdを確認してください。

上記の一連の処理はAWSのサービスを使って行い、AWSにて常時ホストします。